# Frontend

This directory contains the React + Vite client for Mini Notes App.

Main responsibilities:
- registration UI
- login UI
- protected dashboard route
- notes CRUD from the browser
- communication with the Django REST API

Run locally:

```bash
npm install
npm run dev
```

For the full project description, stack, screenshots, and setup instructions, see the root [README](../README.md).
